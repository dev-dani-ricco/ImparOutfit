import { query } from '../config/db.js'; import { HttpError } from '../utils/http.js';
export async function createShowcase(req,res){ const store=(await query('SELECT id FROM stores WHERE owner_user_id=$1',[req.user.id])).rows[0]; if(!store) throw new HttpError(404,'Loja não encontrada'); const b=req.body; const row=(await query('INSERT INTO showcases(store_id,title,description,cover_url,item_ids,starts_at,ends_at) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING *',[store.id,b.title,b.description,b.coverUrl,b.itemIds||[],b.startsAt,b.endsAt])).rows[0]; res.status(201).json(row); }
export async function listShowcases(req,res){ const rows=(await query('SELECT sh.*, s.store_name FROM showcases sh JOIN stores s ON s.id=sh.store_id WHERE ($1::uuid IS NULL OR sh.store_id=$1) ORDER BY sh.created_at DESC',[req.query.storeId||null])).rows; res.json(rows); }
export async function commentShowcase(req,res){ const b=req.body; const row=(await query('INSERT INTO showcase_comments(showcase_id,user_id,rating,comment) VALUES($1,$2,$3,$4) RETURNING *',[req.params.id,req.user.id,b.rating,b.comment])).rows[0]; res.status(201).json(row); }
