import React,{useState} from 'react'; import { View,TextInput,Button,Text,StyleSheet } from 'react-native'; import { useAuth } from '../contexts/AuthContext';
export default function LoginScreen({navigation}){ const {login}=useAuth(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [err,setErr]=useState(''); return <View style={s.v}><Text style={s.logo}>IMPAROutfit</Text><TextInput style={s.i} placeholder="E-mail" value={email} onChangeText={setEmail}/><TextInput style={s.i} placeholder="Senha" secureTextEntry value={password} onChangeText={setPassword}/>{err?<Text>{err}</Text>:null}<Button title="Entrar" onPress={()=>login(email,password).catch(e=>setErr(e.message))}/><Button title="Criar conta" onPress={()=>navigation.navigate('Cadastro')}/></View>; } const s=StyleSheet.create({v:{flex:1,justifyContent:'center',padding:24},logo:{fontSize:34,fontWeight:'800',marginBottom:30},i:{borderWidth:1,borderColor:'#DDD',borderRadius:12,padding:12,marginBottom:12}});
