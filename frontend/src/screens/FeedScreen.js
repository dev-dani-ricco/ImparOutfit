import React,{useEffect,useState} from 'react'; import { ScrollView,Text } from 'react-native'; import { api } from '../api/client'; import { useAuth } from '../contexts/AuthContext'; import Card from '../components/Card';
export default function FeedScreen(){ const {token}=useAuth(); const [rows,setRows]=useState([]); useEffect(()=>{api('/feed',{token}).then(setRows).catch(()=>{})},[]); return <ScrollView><Text style={{fontSize:28,fontWeight:'800',margin:16}}>Feed social</Text>{rows.map(r=><Card key={r.type+r.id} title={r.title} subtitle={`${r.type} • ${r.author}`}/>)}</ScrollView>; }
